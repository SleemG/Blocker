# Adult Content Blocker - Lite Version Requirements
# Minimal dependencies for optimal performance

# Core GUI framework (required)
PyQt5>=5.15.0

# Basic web requests (required)
requests>=2.25.0

# HTML parsing (required)
beautifulsoup4>=4.9.0

# System process monitoring (required)
psutil>=5.8.0

# Optional OCR support (install only if needed)
# Pillow>=8.0.0
# opencv-python>=4.5.0
# pytesseract>=0.3.8

# Note: The lite version works without OCR dependencies
# OCR features will be automatically disabled if not available

# System Requirements:
# - Python 3.7+
# - No additional system packages required for basic functionality
# - Tesseract OCR (optional, only for advanced application monitoring)